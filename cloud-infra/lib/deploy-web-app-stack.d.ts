import { Stack, type StackProps } from 'aws-cdk-lib';
import { Construct } from 'constructs';
export declare class DeployWebAppStack extends Stack {
    constructor(scope: Construct, id: string, props?: StackProps);
}
